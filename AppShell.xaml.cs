using CoffeeShopApp.View;

namespace CoffeeShopApp
{
    public partial class AppShell : Shell
    {
        public AppShell()
        {
            InitializeComponent();

            //Routing.RegisterRoute(nameof(ItemDescriptionPage), typeof(ItemDescriptionPage));

        }
    }
}
